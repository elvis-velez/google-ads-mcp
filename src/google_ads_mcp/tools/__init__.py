"""MCP tool registrations.

Layer 2 (`layer2.py`) holds the two generic escape-hatches: `gaql` for
reads, `mutate` (Phase 2) for writes. Layer 1 (`layer1/`, Phase 3) holds
outcome-shaped wrappers that compose Layer 2.
"""
